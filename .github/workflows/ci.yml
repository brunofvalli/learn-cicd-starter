name: cipipeline

on:
  pull_request:
    branches: [main]

jobs:
  tests:
    name: Tests
    runs-on: self-hosted

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Force Failure
        run: (exit 1)
